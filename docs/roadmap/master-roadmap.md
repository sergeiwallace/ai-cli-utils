# ai-cli-utils Roadmap

## Open

- [ ] `[P0]` `[AI-CLI-53]` **Design/plan doc audit** — review all docs in `docs/designs/` and `docs/plans/`: for unimplemented docs without roadmap tasks, create the task; for completed docs, verify full implementation + robust tests + updated docs. Procedure: `~/projects/sergei/docs/plans/design-plan-doc-audit.md`.

- [x] `[P1]` `[AI-CLI-45]` **Gemini deep-research auth: OAuth client setup + billing resolution** — Track A: `_run_deep_research()` currently returns error on HTTP 403 (insufficient scope) instead of falling through to paid key. Fix: catch 403 on submit and fall through same as OAuth-unavailable path. Track B: set up custom Desktop-app OAuth client in GCP (billing acct `01AC33-5BE8AD-2F4E8A`) so Interactions API can use OAuth scopes (`generative-language.retriever`). Track C: investigate `agent-browser` CLI not found (symlink error, likely not installed). Track D: retry R-12 artelier deep-research run after A or B resolves. Plan: `docs/plans/gemini-deep-research-oauth-plan.md`.

- [x] `[P0]` `[AI-CLI-43]` **Investigate: does Google AI Ultra credit apply to AI Studio paid key charges?** Due: 2026-04-11. — Resolved: billing confirmed working correctly; `-P` / `--confirm-paid` redundancy gate removed 2026-04-17 (deep-research now runs with only `paid_fallback_enabled = true` in config).

- [x] `[P1]` `[AI-CLI-41]` **Overhaul Gemini API cost and gemini-cli usage tracking** Plan: `docs/plans/gemini-usage-tracking-plan.md`. — T-01: tier naming overhaul + token extraction fix + is_deep_research log field. T-02: paid_fallback_enabled config gate + daily DR run counter with warning at 18/20 (--confirm-paid gate removed 2026-04-17). T-03: `ai spend gemini` command — JSONL log stats + BigQuery billing export. T-04 (humanware HW-3): unblocked, pending implementation in humanware repo. Shipped 2026-04-11.

- [x] `[P1]` `[AI-CLI-42]` **Bug: Hetzner sessions use wrong tmux name prefix (c- instead of c-r-)** — When `ai c 1` is run directly on the dev server (`AI_CLI_HOST=hetzner`), the tmux session is named `c-ai-cli-1` instead of `c-r-ai-cli-1`. The `c-r-` prefix is intended for remote/dev server sessions but `ai c` doesn't check `AI_CLI_HOST` when naming the session — it only applies `c-r-` when `-R` is explicitly passed. Fix: detect `AI_CLI_HOST` at session-name time and auto-apply `c-r-` prefix when on the dev server, so the `-R` flag is not required when already there. Related to quota pane discovery which also uses the `c-r-` prefix to find sessions.

- [x] `[P1]` `[AI-CLI-46]` **Stabilize programmatic API for aido integration** — `__all__` added to `gemini.py` (`GeminiResult`, `AttemptLog`, `run_gemini`) and `quota.py` (`QuotaSnapshot`, `read_latest_snapshot`). `read_latest_snapshot()` added — reads latest stored snapshot from SQLite, no tmux scraping. `__init__.__version__` synced to pyproject.toml. 6 smoke tests added in `tests/test_public_api.py`. Design: `aido/docs/designs/ai-cli-integration.md`. Unblocks: AIDO-18.

- [ ] `[P2]` `[AI-CLI-51]` **Create `ai-citation-validator` shared PyPI package** — Extract aido's `citation_validator/` subpackage into a standalone `ai-citation-validator` PyPI package. Unified `CitationResult` data model (D11). Both ai-cli-utils and aido depend on it as a hard dependency; aido's `scripts/citation_validator/` removed in favour of the package. Prerequisite for AI-CLI-50. Design: `docs/designs/citation-validation.md`.

- [ ] `[P2]` `[AI-CLI-50]` **Citation validation for `ai gemini` research output** — Auto-run after `--depth standard/quick` synthesis (Step 4 in research pipeline): lychee dead-link check + arXiv paper existence via S2/CrossRef/OpenAlex + claim body-text verification. Validation non-blocking by default; appends `## Citation Validation` section to output doc + JSON checkpoint. Optional extras: `ai-cli-utils[citation]` for arXiv pipeline, `ai-cli-utils[citation-nli]` for NLI entailment. Separate `ai research validate <file>` command for existing docs. Design: `docs/designs/citation-validation.md`.

- [ ] `[P2]` `[AI-CLI-48]` **NATS integration tests for handoff callbacks** — Write end-to-end tests for `_on_handoff` (signal-watch), `_write_pending_if_claimed` (handoff-drain), and `_drain` (NATS pull) using the live NATS JetStream service already available in CI. Covers the remaining ~3% coverage gap in `main.py`. Priority: especially important for the auto-handoff queue which has known reliability issues (AI-CLI-16). Options: (B) mock the NATS subscription and invoke callbacks directly; (C) publish real JetStream messages and assert side-effects. Option C preferred for handoff-queue bugs.

- [ ] `[P1]` `[AI-CLI-40]` **Geo-aware SSH reverse proxy for `ai gemini`** — Auto-establish SSH reverse SOCKS tunnel through Mac relay when `ai gemini` detects a geo-restricted model (e.g., `deep-research`). Transparent to callers: proxy set up before API call, torn down after. Config: `[gemini.geo_proxy]` in config.toml (relay host, socks port, geo-restricted model list). Prevents repeated manual workarounds for Hetzner geo-blocks. Plan: `docs/plans/geo-aware-proxy-tunnel-plan.md`.

- [x] `[P1]` `[AI-CLI-47]` **Phase 2: Click command groups migration** — Follow-on to AI-CLI-39. Replace sys.argv + argparse hybrid dispatch in `main.py` with Click command groups. Migrate all ~150 `sys.argv`-patching tests to `CliRunner`. Add `--help` at every subcommand level. Keep `ai internal` as a pre-Click fast path. Fix two pre-existing deploy test failures and split test-patch patterns (blocked on AI-CLI-39 merge). Branch: `feature/main-py-refactor` (continue existing branch).

- [x] `[P1]` `[AI-CLI-39]` **Refactor: split main.py into cohesive modules** — Extract 8 subsystems (config, session, iterm2, handoff, transport, tunnel, circus, session_script) from main.py into dedicated modules. Eliminates all deferred import workarounds. Reduces main.py from 3385→~1000 lines. Phase 2 (separate task): migrate sys.argv dispatch to Click command groups. Branch: `feature/main-py-refactor`. Plan: `docs/plans/main-py-refactor-plan.md`.

- [ ] `[P2]` `[AI-CLI-38]` **UAT: VPN-aware transport switching** — Live end-to-end test of `ai c N -R` with Mullvad VPN. Verify: (1) mosh connects when VPN off; (2) enabling Mullvad → `vpn-watch` detects state change → transport loop switches to SSH automatically; (3) disabling Mullvad → switches back to mosh; (4) `~/.local/state/ai-cli-utils/vpn-transitions.log` contains correct JSONL entries; (5) transport state file (`transport-{session}.json`) is written and cleaned up. Blocked on: Mullvad re-enabled on Mac. Design: `docs/designs/vpn-transport-switching.md`.

- [x] `[P1]` `[AI-CLI-36]` **`ai gemini --depth` research tiers** — add `--depth quick|standard|deep` flag with Gemini native grounding as default search, per-step JSON checkpointing + resume, OAuth→REST auto-fallback . Config: `~/.config/ai-cli/research.yaml` . Start with `standard` tier, evaluate on 3-5 runs, then add `deep`. Plan: `docs/plans/ai-gemini-research-depth-tiers.md`.

- [x] `[P3]` `[AI-CLI-13]` **Security hardening — /tmp predictable filenames + UUID validation** — From post-public Opus security audit. H1: validate `session_id_uuid` matches UUID regex before bash f-string interpolation in `get_engine_script`. H2/H3: move `/tmp/cc-exit-*`, `/tmp/cc-resume-prompt-*`, `/tmp/ai-watcher-lock-*`, `/tmp/iterm2-*` files from `/tmp/` to `~/.local/state/ai-cli/` (user-private, eliminates symlink attack surface). Matters for shared multi-user servers; acceptable on single-user workstations. Also: validate `--project` arg contains no path separators (S1 finding).
- [x] `[P2]` `[AI-CLI-15]` **Fix ai sync push/pull for worktree CC dirs** — `sync.py` currently skips any CC state dir containing `--worktrees-` in the name (line ~820). These dirs need to sync with cwd-path translation between Mac (`~/projects/foo/.worktrees/bar`) and Hetzner (`~/projects/foo/.worktrees/bar (remote)`). Dir name translation also needed: `-Users-sergeiwallace-` ↔ `-home-sergei-`. Translate cwd fields in JSONL lines during push/pull.
- [x] `[P1]` `[AI-CLI-21]` **Cross-machine NATS handoff auto-pickup reliability** — Folded into `[AI-CLI-16]`. Cross-machine scenarios and layer attribution testing covered there.
- [x] `[P2]` `[AI-CLI-22]` **Fix quota scraping — hidden tmux pane instead of injecting into active session** — Implemented: `_scrape_usage_hidden_pane()` spins up a hidden `tmux new-window -d`, starts CC with `--dangerously-skip-permissions`, polls for `❯` prompt, sends `/usage`, captures all 4 metrics (`QuotaSnapshot`), kills window. Storage moved to local SQLite (`quota.db`). `quota_status/history/scrape/record` all use local DB — no HTTP dependency. Statusline hook updated to use `ai quota record`. UAT pending: run `ai quota scrape` against a live CC session. Follow-up: Slack webhook setup (see AI-CLI-25), `[AI-CLI-23]`.
- [ ] `[P3]` `[AI-CLI-23]` **Investigate CC source for native usage data API** — The CC CLI source may expose a cleaner way to read quota % without tmux scraping (e.g. a local cache file, HTTP endpoint, or IPC). If found, replace `_scrape_usage_hidden_pane()` entirely. Context: `/usage` slash command likely syncs from Anthropic servers; the sync mechanism or cache location may be accessible directly. Relates to: `[AI-CLI-22]`.
- [ ] `[P3]` `[AI-CLI-49]` **Evaluate notification channels — ntfy vs Discord vs OS** — After using the dual-channel notification system (Discord webhook + ntfy) for a while, evaluate which channel(s) are most effective in practice. Consider: reliability, banner visibility, mobile delivery latency, ease of config for new installs. May result in changing the default channel, dropping one, or promoting OS notifications. Design: `docs/designs/notification-system.md`.
- [x] `[P2]` `[AI-CLI-25]` **Notification system UAT** — Notification system implementation complete (v0.5.0): `Notifier` class with Discord webhook + ntfy + OS native channels, parallel dispatch, OS fallback, `notification_log` SQLite table, `ai notifications log/list` CLI, quota-watch Circus integration, auto-start at session launch. Remaining: live end-to-end UAT — run `ai quota watch start`, trigger a threshold event, verify notification delivered via configured channels, check `ai notifications log`. Design: `docs/designs/notification-system.md`.
- [ ] `[P1]` `[AI-CLI-16]` **Handoff queue reliability — testing and hardening** — End-to-end testing and hardening of the auto-handoff queue system. Covers: (1) same-machine handoff (session A posts → session B picks up automatically), (2) cross-machine handoff (Mac → Hetzner and reverse via NATS + signal-watch), (3) all 5 pickup layers (drain, signal-watch realtime, while-loop restart, Stop hook, UserPromptSubmit hook). Write thorough unit and integration tests for each scenario — mock NATS transport and file layers. Verify `handoff-events.jsonl` shows correct layer attribution. Fix any gaps found. Goal: predictable, well-tested handoff behavior before leaning on it for production workflows. Relates to `[AI-CLI-21]`.
- [ ] `[P1]` `[AI-CLI-17]` **Resolve last 2% coverage gap in main.py** — 52 uncovered lines all in `main.py` (session-launch branches requiring live tmux). Decision needed: add integration-style tests for the remaining branches, or mark unreachable/environment-dependent branches with `# pragma: no cover`. Current coverage: 98% (2803 stmts, 52 missed). Lines: 845-846, 1151-1152, 1493-1494, 1538-1539, 1548-1549, 1557-1558, 1574-1575, 1599, 1604-1614, 1646-1647, 1654-1677, 1886-1887.
- [x] `[P2]` `[AI-CLI-26]` **iTerm2 YAML layout templating system** — Folded into `[AI-CLI-27]` Phase 3. Design: `docs/designs/iterm2-layout-system.md`.
- [x] `[P1]` `[AI-CLI-27]` **iTerm2 visual identity + workspace overhaul** — Full overhaul of the iTerm2 integration. Supersedes `[AI-CLI-18]` and `[AI-CLI-20]`. Designs: `docs/designs/iterm2-title-color-system.md`, `docs/designs/iterm2-layout-system.md`. Implemented 2026-04-04.
  - **Phase 1 — Visual identity system** ✅: Lease-file collision-free color assignment; Pillow runtime PNG icon generation (Claude, Gemini, shell, caffeinate, Chrome, SSH); auto-contrast tint from tab background via HSL; `[iterm2.icon_color_overrides]` config; OSC 1 title fix (no `[mosh]` prefix); `ai color <name>` ad hoc slot reassignment; EXIT trap cleanup via `ai internal cleanup-session-files`.
  - **Phase 2 — Per-project/session color preferences** ✅: `[iterm2.project_colors]` in `iterm2.toml` maps project/session name → palette color name. Preferred slot pinned; falls back to lowest free slot.
  - **Phase 3 — YAML layout system** ✅: `~/.config/iterm2/layouts/*.yaml` layout files. Pydantic schema (Layout/Tab/Pane/PaneSplit). Dynamic Profile + icon pipeline per tab. `ai layout list/validate/profiles/<name>` CLI.
- [x] `[P1]` `[AI-CLI-20]` **iTerm2 UX redesign** — Superseded by `[AI-CLI-27]`. Issues documented: session name field reverts, copy mode on launch, no ad hoc color control, shell title option lost on profile switch. All carried into AI-CLI-27 Phase 1.
- [x] `[P1]` `[AI-CLI-18]` **iTerm2 title/color system — UAT** — Superseded by `[AI-CLI-27]`. Prior partial implementation at commit `3332168` superseded by full redesign (runtime Pillow icons, lease-file colors, OSC 1 fix).
- [x] `[P2]` `[AI-CLI-28]` **Process hygiene — stale process detection and cleanup** — Add `ai ps` command showing all ai-cli-managed processes (mosh-server orphans, signal-watch, circus workers, NATS, autossh tunnels) with age and status. Add `ai ps clean` to kill orphaned processes (mosh-server with no active client, stale signal-watch, etc.). Run on both local Mac and remote Hetzner via SSH. Consider running `ai ps clean` automatically at session start or on a cron. Triggered by: 5 orphaned mosh-server processes found on Mac from sessions started in March 2026.
- [ ] `[P3]` `[AI-CLI-11]` **Logo polish — increase visual weight** — All elements (nodes, edges, arc eye, chevron, underscore) appear pencil-thin vs other ecosystem logos (e.g. Python). Increase stroke widths and node radii so the logo holds up at small sizes and alongside other icons.
- [x] `[P0]` `[AI-CLI-3]` **Terminal demo GIF** — Placeholder MP4 recording embedded in README. Better recording tracked in AI-CLI-52.

- [ ] `[P2]` `[AI-CLI-52]` **Re-record demo video for README and portfolio** — Current recording (`demo-20260420-053045-1cdf560.mp4`) is a manual placeholder. Produce a clean, well-timed demo video showing the core workflow: session launches with colored tabs and icons, `ai ls`, `ai quota status`, `ai gemini` with auth fallback, remote session. Targets: (1) GitHub README embed, (2) personal site portfolio page. Use the automated `record-demo.sh` + `demo-runner.sh` pipeline once timing is reliable, or re-record manually in a controlled layout.
- [x] `[P1]` `[AI-CLI-30]` **Privacy audit — scrub proprietary and personal references** — Scrub all private names (humanware, aido, sergei/sergeiwallace, 178.104.70.139) from src/, tests/, docs/ before version bump. Fix functional violations (hardcoded Doppler project, NATS topics, SSH defaults). Rename `_is_humanware_platform()` to generic equivalent. Audit command: `git grep -rn "humanware\|aido\|\bsergei\b\|sergeiwallace\|178\.104" -- src/ tests/`. Gate: zero hits in src/+tests/. Plan: `docs/plans/pre-release-v0.2.0-plan.md` Phase 3.
- [x] `[P1]` `[AI-CLI-31]` **CLAUDE.md / GEMINI.md alignment enforcement** — Write a lint script that extracts the shared sections from both files and fails CI if they drift. Shared sections (must be identical): public package standards, CLI conventions, test requirements, pragma gate, dev workflow, versioning convention, doc maintenance. Agent-specific sections allowed to diverge: CLAUDE.md keeps hooks/skills/MCP tool policies/agent spawning; GEMINI.md keeps browser automation role, token-saving heuristics. Scoped to this repo only — cross-project propagation is a separate platform task.
- [ ] `[P2]` `[AI-CLI-32]` **CLI ergonomics and first-run UX audit** — Walk through the tool as a new user on each platform (macOS, Linux, Windows). Verify `ai setup` works end-to-end from a fresh `pip install`. Identify confusing command names, missing help text, awkward flows, or manual steps that survive the setup process. Fix gaps found. Gate: a developer unfamiliar with the codebase can install and use core commands from the README alone.
- [ ] `[P2]` `[AI-CLI-29]` **Windows out-of-box support** — Package should install and run on Windows without manual environment variable configuration. Covers: OS-aware default paths (`%APPDATA%`, `%LOCALAPPDATA%` instead of `~/.config`/`~/.local/state`), Windows-compatible subprocess calls (no bash-specific shell features), config defaults that don't assume POSIX paths, and CI matrix expansion to include Windows. Currently non-functional on Windows due to hardcoded POSIX assumptions and env var requirements.
- [x] `[P1]` `[AI-CLI-34]` **CDP browser debug server — `ai cdp start/stop/status`** — Launch and kill a Chrome/Chromium instance with `--remote-debugging-port` exposed for Playwright, agent-browser, and other CDP tools. `ai cdp start [--port 9222] [--no-incognito]` launches Chrome in the background, waits up to 5s for the endpoint to respond, prints "CDP ready at localhost:<port>". `ai cdp stop [--port 9222]` kills by PID file. PID file at `~/.local/state/ai-cli-utils/cdp-<port>.pid`. Chrome binary auto-detected across macOS/Linux/Windows; overridable via `[cdp] binary_path`. Plan: `docs/plans/cdp-plan.md`.
- [x] `[P1]` `[AI-CLI-35]` **Bug: prompt injection unsafe — triggers rewind UI and cuts off mid-typing** — The `tmux send-keys` injection used by the auto-restart/session-resume mechanism is not safe to fire into an active pane. Two reported symptoms: (1) injection triggers CC's conversation rewind option display on session start; (2) `/exit` (or similar injected text) lands while user is mid-typing and truncates their prompt. Root cause is likely the same: `send-keys` fires without checking whether the pane is ready and idle. Investigate: pane-readiness guard before injection, timing of the injection relative to CC startup, and whether a different injection mechanism avoids both issues. Reported 2026-04-05.
- [x] `[P2]` `[AI-CLI-37]` **Automate iTerm2 Shift+Enter key binding via DynamicProfile** — Currently documented as a manual import step (`assets/iterm2-key-bindings/shift_enter_cc_new_line_iterm2_key_binding.itermkeymap`). Instead, inject `"Key Mappings": {"0xd-0x20000-0x24": {...}}` directly into the per-session DynamicProfile JSON in `generate_dynamic_profile()` (`icon_generator.py`). CC sessions will get the binding automatically on launch; no user import step needed. Delete `assets/iterm2-key-bindings/` and update `docs/tools/iterm2-setup.md` to remove the manual step. Plan: `docs/plans/pre-release-v0.2.0-plan.md`.
- [ ] `[P3]` `[AI-CLI-33]` **Configurable AI provider support** — Extend session management to support providers beyond Claude and Gemini (e.g. OpenAI, GitHub Copilot). Provider should be configurable — icon, tab color, session naming, and launch command all driven by a provider config block. No hardcoded Claude/Gemini assumptions in core session logic. Target: adding a new provider requires only config, not code changes.
- [x] `[P3]` `[AI-CLI-44]` **Enforce CHANGELOG.md entry via commit-msg hook** — Add `.githooks/commit-msg` that blocks `feat:` and `feat(scope):` commits unless `CHANGELOG.md` is staged in the same changeset. Checks first line only (body mentions of feat: do not trigger). `SKIP_CHANGELOG=1` escape hatch. Tests in `tests/hooks/test-commit-msg.sh` (must unset `GIT_DIR`/`GIT_INDEX_FILE`/`GIT_WORK_TREE` to isolate temp repos from pre-push hook context). Wire hook tests into pre-push gate. Update `install.sh` to document the new hook. Reference: ai-ide-mobile `6efd310` for reference implementation. Note: this project is Python with `pyproject.toml` as the version source — adapt as needed for any Python-specific considerations.
- [ ] `[P3]` `[AI-CLI-7]` **Release Drafter** — Auto-draft GitHub Release notes from PR labels. Add when project gets regular external PRs.
- [x] `[P3]` `[AI-CLI-8]` **pyright type checking in CI** — pyright basic mode added to CI lint job. Fixed 11 type errors across gemini.py, main.py, memory.py, messaging.py, sync.py.

## Completed

- [x] `[P1]` `[AI-CLI-14]` **NATS cross-session signaling** — Dual-layer (NATS push + file queue durability). `ai internal signal-watch` subscribes durable, atomically claims tasks, writes pending-file for auto-pickup. SSH tunnel auto-opened on Mac. `ai handoff post --remote` SSHes to Hetzner. Bash template auto-launches signal-watch alongside sync/memory watch. Plan: `docs/plans/cross-session-signaling-plan.md`.
- [x] `[P2]` `[AI-CLI-12]` **Coverage push to ~98%** — 734 tests, 98% line coverage (52 uncovered lines in main.py session-launch branches). Added ai ls/attach features, test quality audit + fixes, CI isolation fixes, `ai setup` command + 24 tests. Plan: `docs/plans/going-public-plan.md` § AI-CLI-12.
- [x] `[P1]` `[AI-CLI-10]` **Create developer email + update SECURITY.md** — Create a dedicated developer email for security reports. Update SECURITY.md contact info. Also update pyproject.toml author email and any other references to personal email.
- [x] `[P1]` `[AI-CLI-9]` **Flip repo public** — Install Renovate App, set up Codecov token, flip to public, enable branch rulesets + CodeQL + secret scanning, social preview image, verify badges + external clone. 80% test coverage + Codecov badge added.
- [x] `[P1]` `[AI-CLI-6]` **Going public — repo automation & hardening** — CI matrix (3.11/3.12/3.13), Codecov, GH Release automation, pre-commit, Renovate (replace Dependabot), issue/PR templates, secret scan, GH Release v0.1.1. Plan: `docs/plans/going-public-plan.md`.
- [x] `[P1]` `[AI-CLI-1]` **Fix lint + test failures and ship v0.1.1** — 13 ruff lint issues + 7 test failures. v0.1.1 on PyPI.
- [x] `[P2]` `[AI-CLI-2]` **Full README rewrite** — PyOpenSci 13-section structure with badges, features table, usage reference, config example, quick start.
- [x] `[P3]` `[AI-CLI-4]` **CONTRIBUTING.md** — Dev setup, testing, code style, PR process, project structure.
- [x] `[P1]` `[AI-CLI-5]` **Professionalize for open-source release** — T-00 through T-08: generalize code (remove all hardcoded personal references), CI/publish workflows, badges, CHANGELOG, community files (SECURITY.md, dependabot), make projects_dir configurable, Trusted Publisher + project-scoped PyPI token. Plan: `docs/plans/open-source-professionalization-plan.md`. Research: R-35.
