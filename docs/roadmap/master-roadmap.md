# ai-cli-utils Roadmap

## Open

- [x] `[P3]` `[AI-CLI-13]` **Security hardening — /tmp predictable filenames + UUID validation** — From post-public Opus security audit. H1: validate `session_id_uuid` matches UUID regex before bash f-string interpolation in `get_engine_script`. H2/H3: move `/tmp/cc-exit-*`, `/tmp/cc-resume-prompt-*`, `/tmp/ai-watcher-lock-*`, `/tmp/iterm2-*` files from `/tmp/` to `~/.local/state/ai-cli/` (user-private, eliminates symlink attack surface). Matters for shared multi-user servers; acceptable on single-user workstations. Also: validate `--project` arg contains no path separators (S1 finding).
- [x] `[P2]` `[AI-CLI-15]` **Fix ai sync push/pull for worktree CC dirs** — `sync.py` currently skips any CC state dir containing `--worktrees-` in the name (line ~820). These dirs need to sync with cwd-path translation between Mac (`/Users/sergeiwallace/projects/foo/.worktrees/bar`) and Hetzner (`/home/sergei/projects/foo/.worktrees/bar`). Dir name translation also needed: `-Users-sergeiwallace-` ↔ `-home-sergei-`. Translate cwd fields in JSONL lines during push/pull.
- [ ] `[P1]` `[AI-CLI-21]` **Cross-machine NATS handoff auto-pickup reliability** — Track testing, edge cases, and fixes for the 5-layer handoff pickup system across Mac ↔ Hetzner. Covers: drain layer, signal-watch realtime layer, while-loop restart, Stop hook, UserPromptSubmit hook. Verify handoff-events.jsonl shows correct layer attribution. Current status: drain layer fixed (claimed-dir guard), test handoffs #8/#9 posted. Next: validate Mac realtime pickup via signal-watch → handoff_pending_file path.
- [x] `[P2]` `[AI-CLI-22]` **Fix quota scraping — hidden tmux pane instead of injecting into active session** — Implemented: `_scrape_usage_hidden_pane()` spins up a hidden `tmux new-window -d`, starts CC with `--dangerously-skip-permissions`, polls for `❯` prompt, sends `/usage`, captures all 4 metrics (`QuotaSnapshot`), kills window. Storage moved to local SQLite (`quota.db`). `quota_status/history/scrape/record` all use local DB — no HTTP dependency. Statusline hook updated to use `ai quota record`. UAT pending: run `ai quota scrape` against a live CC session. Follow-up: Slack webhook setup (see AI-CLI-25), `[AI-CLI-23]`.
- [ ] `[P3]` `[AI-CLI-23]` **Investigate CC source for native usage data API** — The CC CLI source may expose a cleaner way to read quota % without tmux scraping (e.g. a local cache file, HTTP endpoint, or IPC). If found, replace `_scrape_usage_hidden_pane()` entirely. Context: `/usage` slash command likely syncs from Anthropic servers; the sync mechanism or cache location may be accessible directly. Relates to: `[AI-CLI-22]`.
- [ ] `[P2]` `[AI-CLI-25]` **Quota Slack webhook setup and UAT** — (1) Create incoming Slack webhook, store URL in Doppler under ai-cli-utils config as `slack_webhook_url` under `[quota]` section. (2) Run live UAT: `ai quota scrape` with a CC session open — verify correct % extracted and stored in `~/.local/state/ai-cli/quota.db`. (3) Verify `ai quota status` and `ai quota history` read from local DB. (4) Humanware side: make `quota.py` routes thin wrappers reading from ai-cli DB; remove 3 quota tables from `db.py` (test_telemetry.py and test_quota_routes.py need to adapt to read from ai-cli DB path). Design: `docs/designs/claude-usage-telemetry.md`.
- [ ] `[P0]` `[AI-CLI-16]` **Handoff observability checkpoint** — Due: 2026-04-04. Review `~/.local/state/ai-cli/handoff-events.jsonl` logs. Check: are handoffs being picked up reliably across all 5 layers? Any stale tasks that weren't flagged by banners? Any send-keys nudge failures? Gap analysis across all mechanisms (Stop hook, UserPromptSubmit hook, send-keys nudge, while-loop restart, startup scan). Document findings and fix any gaps. Hard checkpoint to validate auto-handoff reliability before leaning on it for production workflows.
- [ ] `[P1]` `[AI-CLI-17]` **Resolve last 2% coverage gap in main.py** — 52 uncovered lines all in `main.py` (session-launch branches requiring live tmux). Decision needed: add integration-style tests for the remaining branches, or mark unreachable/environment-dependent branches with `# pragma: no cover`. Current coverage: 98% (2803 stmts, 52 missed). Lines: 845-846, 1151-1152, 1493-1494, 1538-1539, 1548-1549, 1557-1558, 1574-1575, 1599, 1604-1614, 1646-1647, 1654-1677, 1886-1887.
- [x] `[P2]` `[AI-CLI-26]` **iTerm2 YAML layout templating system** — Folded into `[AI-CLI-27]` Phase 3. Design: `docs/designs/iterm2-layout-system.md`.
- [ ] `[P1]` `[AI-CLI-27]` **iTerm2 visual identity + workspace overhaul** — Full overhaul of the iTerm2 integration. Supersedes `[AI-CLI-18]` and `[AI-CLI-20]`. Designs: `docs/designs/iterm2-title-color-system.md`, `docs/designs/iterm2-layout-system.md`. Three phases:
  - **Phase 1 — Visual identity system**: Lease-file collision-free color assignment; Pillow runtime PNG icon generation for all profile types (Claude, Gemini, shell, caffeinate, Chrome, SSH); auto-contrast tint from tab background via HSL color theory; optional `icon_color` override per palette entry; OSC 1 title fix (no `[mosh]` prefix); Gemini `os.chdir` fix for local `-p` sessions; `ai color <name>` ad hoc slot reassignment; fix tmux copy mode on launch; fix Session Name field revert on Edit Session. Source logos at `src/ai_cli/data/icons/`.
  - **Phase 2 — Per-project/session color preferences** (`[AI-CLI-24]`): `[iterm2.project_colors]` in `iterm2.toml` maps project/session name → palette color name. Preferred slot pinned; falls back to lowest free slot if occupied.
  - **Phase 3 — YAML layout system** (`[AI-CLI-26]`): File-driven window/tab/pane layout definitions in `~/.config/iterm2/layouts/*.yaml`. Nested pane split model. Dynamic Profile generation + icon pipeline per tab. `ai layout <name/list/validate/profiles>` CLI. `ai c N` auto-applies matching layout if `sw-N.yaml` exists.
- [x] `[P1]` `[AI-CLI-20]` **iTerm2 UX redesign** — Superseded by `[AI-CLI-27]`. Issues documented: session name field reverts, copy mode on launch, no ad hoc color control, shell title option lost on profile switch. All carried into AI-CLI-27 Phase 1.
- [x] `[P1]` `[AI-CLI-18]` **iTerm2 title/color system — UAT** — Superseded by `[AI-CLI-27]`. Prior partial implementation at commit `3332168` superseded by full redesign (runtime Pillow icons, lease-file colors, OSC 1 fix).
- [ ] `[P3]` `[AI-CLI-11]` **Logo polish — increase visual weight** — All elements (nodes, edges, arc eye, chevron, underscore) appear pencil-thin vs other ecosystem logos (e.g. Python). Increase stroke widths and node radii so the logo holds up at small sizes and alongside other icons.
- [ ] `[P2]` `[AI-CLI-3]` **Terminal demo GIF** — `demo/demo.tape` exists. Split: cc session reviews/updates the tape script; human runs `vhs demo/demo.tape` on Mac (needs display) and embeds GIF in README.
- [ ] `[P3]` `[AI-CLI-7]` **Release Drafter** — Auto-draft GitHub Release notes from PR labels. Add when project gets regular external PRs.
- [x] `[P3]` `[AI-CLI-8]` **pyright type checking in CI** — pyright basic mode added to CI lint job. Fixed 11 type errors across gemini.py, main.py, memory.py, messaging.py, sync.py.

## Completed

- [x] `[P1]` `[AI-CLI-14]` **NATS cross-session signaling** — Dual-layer (NATS push + file queue durability). `ai internal signal-watch` subscribes durable, atomically claims tasks, writes pending-file for auto-pickup. SSH tunnel auto-opened on Mac. `ai handoff post --remote` SSHes to Hetzner. Bash template auto-launches signal-watch alongside sync/memory watch. Plan: `docs/plans/cross-session-signaling-plan.md`.
- [x] `[P2]` `[AI-CLI-12]` **Coverage push to ~98%** — 734 tests, 98% line coverage (52 uncovered lines in main.py session-launch branches). Added ai ls/attach features, test quality audit + fixes, CI isolation fixes, `ai setup` command + 24 tests. Plan: `docs/plans/going-public-plan.md` § AI-CLI-12.
- [x] `[P1]` `[AI-CLI-10]` **Create developer email + update SECURITY.md** — Create a dedicated developer email for security reports. Update SECURITY.md contact info. Also update pyproject.toml author email and any other references to personal email.
- [x] `[P1]` `[AI-CLI-9]` **Flip repo public** — Install Renovate App, set up Codecov token, flip to public, enable branch rulesets + CodeQL + secret scanning, social preview image, verify badges + external clone. 80% test coverage + Codecov badge added.
- [x] `[P1]` `[AI-CLI-6]` **Going public — repo automation & hardening** — CI matrix (3.11/3.12/3.13), Codecov, GH Release automation, pre-commit, Renovate (replace Dependabot), issue/PR templates, secret scan, GH Release v0.1.1. Plan: `docs/plans/going-public-plan.md`.
- [x] `[P1]` `[AI-CLI-1]` **Fix lint + test failures and ship v0.1.1** — 13 ruff lint issues + 7 test failures. v0.1.1 on PyPI.
- [x] `[P2]` `[AI-CLI-2]` **Full README rewrite** — PyOpenSci 13-section structure with badges, features table, usage reference, config example, quick start.
- [x] `[P3]` `[AI-CLI-4]` **CONTRIBUTING.md** — Dev setup, testing, code style, PR process, project structure.
- [x] `[P1]` `[AI-CLI-5]` **Professionalize for open-source release** — T-00 through T-08: generalize code (remove all hardcoded personal references), CI/publish workflows, badges, CHANGELOG, community files (SECURITY.md, dependabot), make projects_dir configurable, Trusted Publisher + project-scoped PyPI token. Plan: `docs/plans/open-source-professionalization-plan.md`. Research: R-35.
